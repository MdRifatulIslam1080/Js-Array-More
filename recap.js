const books = ["Physics", "Chemistry", "Math", "Biology", "English", "Bangla"];
for (let i = 0; i < books.length; i++) {
  console.log(books[i]);
}

const rev_books = books.reverse();
console.log(rev_books);
const sorted_books = books.sort();
console.log(sorted_books);
